# Практическая работа

## Тема: Мониторинг с Prometheus и Grafana

### Цель работы

1. Развернуть простую мониторинговую систему с использованием Prometheus и Grafana.
2. Настроить сбор метрик с одного экспортера (например, node_exporter) и визуализацию в Grafana.
3. Познакомиться с архитектурой, конфигурацией, возможностями и связями между компонентами.
4. Составить собственный дашборд и произвести базовую настройку алертов.

### Материалы и ссылки

* Официальная документация Prometheus — «Getting started». ([Prometheus][1])
* Официальная документация Grafana — введение и подключение источника данных Prometheus. ([Grafana Labs][2])
* Подключение Grafana к Prometheus как источнику данных. ([Prometheus][3])

---

## Архитектура системы мониторинга

### Общая идея

Система мониторинга на базе **Prometheus + Grafana** — это классический стек для сбора, хранения и визуализации метрик работы инфраструктуры, сервисов и приложений.
Основная цель такой архитектуры — **обеспечить наблюдаемость (observability)**: видеть текущее состояние системы, замечать отклонения и предупреждать инциденты заранее.

---

### Схема архитектуры

![Prometheus + Grafana Architecture](https://devopscube.com/content/images/2025/03/prometheus-architecture.gif)

*(Источник: [DevOpsCube, 2025](https://devopscube.com/prometheus-architecture/))*

---

### Компоненты архитектуры

#### 1. **Prometheus Server**

Центральный элемент всей системы мониторинга.
Он выполняет **три ключевые функции**:

* **Сбор метрик (Scraping):** Prometheus регулярно обращается к источникам метрик (экспортерам или самим приложениям), используя HTTP-запросы по протоколу *pull* (тянет данные сам).
* **Хранение данных:** метрики сохраняются во внутренней базе данных — **TSDB (Time Series Database)**. Это высокоэффективное хранилище временных рядов.
* **Оценка правил и алертов:** Prometheus выполняет выражения и триггеры для отправки оповещений, используя язык **PromQL (Prometheus Query Language)**.

📘 *Подробнее:* [Prometheus Architecture — Official Docs][4]

---

#### 2. **Экспортеры (Exporters)**

Prometheus сам не может собирать данные о внешних системах напрямую — для этого нужны **экспортеры**.
Экспортер — это небольшое приложение (или агент), которое:

* собирает метрики из системы/приложения (например, ОС, Docker, БД);
* преобразует их в формат, понятный Prometheus;
* предоставляет метрики через HTTP-эндпоинт `/metrics`.

Примеры распространённых экспортёров:

| Экспортер           | Назначение                                         |
| ------------------- | -------------------------------------------------- |
| `node_exporter`     | Мониторинг состояния ОС (CPU, память, сеть, диски) |
| `cAdvisor`          | Метрики Docker-контейнеров                         |
| `blackbox_exporter` | Проверка доступности HTTP/ICMP-ресурсов            |
| `mysql_exporter`    | Метрики MySQL-базы                                 |
| `jmx_exporter`      | Метрики Java-приложений через JMX                  |

📘 *Подробнее:* [Prometheus Exporters List](https://prometheus.io/docs/instrumenting/exporters/)

---

#### 3. **Alertmanager (опционально)**

Отдельный сервис, который принимает оповещения (**alerts**) от Prometheus и управляет их доставкой.

**Функции Alertmanager:**

* Группировка и подавление повторяющихся алертов;
* Настройка маршрутов доставки (email, Slack, Telegram и т.д.);
* Поддержка шаблонов уведомлений.

Prometheus может работать и без Alertmanager — но в промышленной эксплуатации он необходим.

📘 *Подробнее:* [Alertmanager Documentation](https://prometheus.io/docs/alerting/latest/alertmanager/)

---

#### 4. **Grafana**

Grafana — это визуализационная платформа, которая подключается к Prometheus как к источнику данных.

**Возможности Grafana:**

* Строит интерактивные дашборды на основе запросов PromQL;
* Позволяет объединять данные из разных источников (Prometheus, Loki, InfluxDB, Elasticsearch);
* Может сама управлять алертами (начиная с Grafana v8+);
* Поддерживает шаблоны и общий доступ к дашбордам.

📘 *Подробнее:* [Grafana Prometheus Integration][5]

---

#### 5. **Targets (цели мониторинга)**

Prometheus конфигурируется на определённый список целей (targets), которые нужно опрашивать.
Каждая цель — это HTTP-endpoint, возвращающий метрики в формате:

```
# HELP node_cpu_seconds_total Seconds the CPUs spent in each mode.
# TYPE node_cpu_seconds_total counter
node_cpu_seconds_total{cpu="0",mode="user"} 212.45
```

В конфигурации `prometheus.yml` они описываются так:

```yaml
scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['node_exporter:9100']
```

---

#### 6. **TSDB (Time Series Database)**

Prometheus хранит все метрики в своей **встроенной временной базе данных**.
Особенности TSDB:

* Использует модель данных *метка-значение (label-based)*;
* Каждая метрика — это временной ряд (`time series`);
* Позволяет быстро фильтровать и агрегировать данные по любым меткам.

Пример временного ряда:

```
http_requests_total{method="GET", handler="/api"}  11234
```

---

#### 7. **Пользователь / Аналитик / DevOps-инженер**

Конечный потребитель данных мониторинга:

* получает визуальные дашборды;
* реагирует на алерты;
* оптимизирует систему на основе метрик.

---

### Объяснение схемы

1. **Prometheus Server** регулярно (например, каждые 15 секунд) делает HTTP-запросы к экспортёрам и собирает данные.
2. **Экспортеры** предоставляют системные или прикладные метрики через `/metrics`.
3. **Prometheus** сохраняет полученные данные во временной базе TSDB и делает их доступными через API.
4. **Grafana** обращается к API Prometheus, выполняя запросы на языке PromQL, и визуализирует результаты в виде графиков и панелей.
5. При срабатывании определённых условий **Prometheus** отправляет алерты в **Alertmanager**, который маршрутизирует их к нужным каналам.
6. Пользователи наблюдают за системой через интерфейс Grafana, анализируют показатели и принимают решения.

---

### Упрощённая схема потоков данных

```text
[ node_exporter ] --->  (метрики)  ---> [ Prometheus Server ] ---> [ Grafana Dashboard ]
                                    \--> [ Alertmanager ] ---> [ Email/Slack/etc. ]
```

---

### Почему такая архитектура эффективна

| Преимущество                 | Объяснение                                                              |
| ---------------------------- | ----------------------------------------------------------------------- |
| **Децентрализация**          | Каждое приложение может иметь свой экспортёр, не влияя на другие.       |
| **Гибкость pull-модели**     | Prometheus сам решает, когда собирать данные, без нагрузки на цели.     |
| **Лёгкость масштабирования** | Можно добавить новые экспортеры и сервисы без остановки системы.        |
| **Совместимость**            | Grafana поддерживает множество источников данных, не только Prometheus. |
| **Открытые стандарты**       | Формат метрик и API Prometheus — де-факто стандарт индустрии.           |

---

### Пример архитектуры для Big Data-кластеров

Для больших распределённых систем (например, Spark, Hadoop, Kafka):

* Каждый узел кластера имеет свой `node_exporter`;
* Сервисы (Spark Master, Kafka Broker, Zookeeper) публикуют свои метрики через `jmx_exporter`;
* Один или несколько Prometheus-серверов собирают метрики;
* Grafana объединяет визуализацию для всей инфраструктуры.

![Grafana + Prometheus for Big Data](https://kodekloud.com/blog/content/images/2023/06/grafana-prometheus-1.JPG)

*(Источник: [KodeKloud, 2023](https://kodekloud.com/blog/grafana-prometheus-integration/))*

Архитектура Prometheus + Grafana построена по принципам:

* **Наблюдаемости (Observability);**
* **Масштабируемости (Scalability);**
* **Автоматизации и расширяемости (Extensibility).**

---

### Сценарий выполнения

1. Запустите Docker-контейнеры Prometheus и Grafana с помощью Docker-Compose.
2. Настройте Prometheus: конфигурационный файл `prometheus.yml`, задать цели (targets) на node_exporter.
3. Настройте Grafana: подключите источник данных Prometheus.
4. Подключите/запустите exporter (например, node_exporter) на хостовой машине или в контейнере.
5. В Grafana создайте простой дашборд: например, график загрузки CPU с node_exporter.
6. (Необязательно) Настройте правило алерта в Prometheus, проверьте, что оно срабатывает.
7. Сделайте скриншот/экспорт дашборда, напишите пояснение по метрикам, интерпретации.

### Конфигурационные файлы и Docker-Compose

#### Файл: `docker-compose.yml`

```yaml
version: '3.8'

services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    command:
      - "--storage.tsdb.retention.time=7d"
      - "--web.enable-lifecycle"   # позволяет reload конфигурации
    networks:
      - monitoring

  node_exporter:
    image: prom/node-exporter:latest
    container_name: node_exporter
    ports:
      - "9100:9100"
    networks:
      - monitoring
    # Для мониторинга хоста:
    # volumes:
    #   - /proc:/host/proc:ro
    #   - /sys:/host/sys:ro
    #   - /:/rootfs:ro
    # environment:
    #   - HOST_ROOT=/rootfs

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    networks:
      - monitoring

networks:
  monitoring:
    driver: bridge
```

#### Файл: `prometheus.yml`

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']

  - job_name: 'node_exporter'
    static_configs:
      - targets: ['node_exporter:9100']
```

### Инструкции по запуску

1. Сохраните файлы `docker-compose.yml` и `prometheus.yml` в одну папку, например `monitoring_lab`.
2. В терминале перейдите в эту папку.
3. Запустите команду:

   ```bash
   docker-compose up ­-d
   ```

   Контейнеры Prometheus, node_exporter и Grafana будут запущены.
4. Откройте браузер и перейдите на `http://localhost:9090` — интерфейс Prometheus.
5. Откройте `http://localhost:3000` — интерфейс Grafana. Логин: `admin`, пароль: `admin` (или тот, который вы указали).
6. В Grafana:

   * Перейдите **Configuration → Data Sources → Add data source**.
   * Выберите «Prometheus».
   * URL: `http://prometheus:9090` (если Grafana в том же Docker-Compose) или `http://localhost:9090`. ([Prometheus][3])
   * Нажмите **Save & Test** — должно быть «Data source is working».
7. Создайте новый Dashboard → добавьте Panel → в запросе используйте, например:

   ```
   node_cpu_seconds_total{mode="idle"}
   ```

   или

   ```
   rate(node_cpu_seconds_total{mode!="idle"}[1m])
   ```

   Настройте отображение, промаркируйте оси.
8. Проверьте, что metrika отображается.
9. (Дополнительно) В Prometheus добавьте правило алерта в секцию `alerting`. Например:

   ```yaml
   rule_files:
     - "alert_rules.yml"
   ```

   А в файле `alert_rules.yml`:

   ```yaml
   groups:
     - name: example_alerts
       rules:
         - alert: HighCPU
           expr: rate(node_cpu_seconds_total{mode!="idle"}[2m]) > 0.90
           for: 2m
           labels:
             severity: warning
           annotations:
             summary: "Высокая нагрузка CPU"
             description: "CPU usage >90% на узле {{ $labels.instance }}"
   ```

   Перезапустите Prometheus или используйте `/-/reload` endpoint, если включён `--web.enable-lifecycle`.
10. Проверить Alerts → View in Alertmanager (если настроен) или вкладка Alerts в Grafana (если используется).
11. Сделайте скриншоты: интерфейс Prometheus, интерфейс Grafana с дашбордом, данные метрики, срабатывание алерта.
12. Напишите краткий отчёт: какие метрики вы собрали, что они означают, какие выводы можно сделать.

### Контрольные вопросы

1. Почему используется модель *pull* (Prometheus «scrape» метрик) и какие преимущества/ограничения у неё?
2. Для чего в Prometheus используется `scrape_interval` и `evaluation_interval`?
3. Что такое exporter и почему мы не просто запускали приложение без него?
4. Чем полезен Grafana как инструмент визуализации, и какая роль дашбордов в мониторинге?
5. Какие узкие места или ошибки конфигурации могут привести к тому, что метрики не появляются или отображаются неправильно?
6. Как бы вы расширили эту систему мониторинга (например, для распределённой Big Data-системы)?

---

### Заключение

Отлично 👍
Вот переработанный блок **«Заключение»** — ссылки оформлены в виде обычного списка, чтобы корректно рендерились в любом Markdown-редакторе (GitHub, Obsidian, VS Code, Notion и т.д.):

---

### Заключение

Для самостоятельного изучения и закрепления материала рекомендуется ознакомиться с официальной документацией и дополнительными ресурсами:

* 📘 **Prometheus — Getting Started**
  [https://prometheus.io/docs/prometheus/latest/getting_started/](https://prometheus.io/docs/prometheus/latest/getting_started/)

* 📗 **Prometheus — Overview (Обзор архитектуры)**
  [https://prometheus.io/docs/introduction/overview/](https://prometheus.io/docs/introduction/overview/)

* 📘 **Prometheus + Grafana Integration**
  [https://prometheus.io/docs/visualization/grafana/](https://prometheus.io/docs/visualization/grafana/)

* 📙 **Grafana Labs — Fundamentals (Основы работы и интерфейс)**
  [https://grafana.com/docs/grafana/latest/fundamentals/](https://grafana.com/docs/grafana/latest/fundamentals/)

* 📒 **Grafana — Prometheus Data Source Setup**
  [https://grafana.com/docs/grafana/latest/datasources/prometheus/](https://grafana.com/docs/grafana/latest/datasources/prometheus/)

